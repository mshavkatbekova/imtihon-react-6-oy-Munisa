import { useEffect, useState } from "react";
import { useFetch } from "../hooks/useFetch";

function SingleMovies({ id }) {
  return <dialog id="my_modal_2" className="modal"></dialog>;
}

export default SingleMovies;
